# 部署 - 背单词应用

本应用为纯前端静态站点 (HTML + CSS + JS)，无服务器端，可部署到任何免费静态托管平台。
数据存储在 Supabase 云端，与部署平台无关。

---

## 平台对比

> Cloudflare 官网在部分地区可能访问受阻，Gitee Pages 免费版已于 2024 年 5 月永久下线。

| 平台 | 国内访问 | 免备案 | 需要 Git | 免费额度 | 注册难度 |
|------|---------|--------|---------|---------|---------|
| **腾讯云 CloudBase** ⭐首选 | 极快 | 是 | 否（网页上传） | 1GB 存储 / 5GB 流量/月 | 需实名（微信即可） |
| **AxureShow** ⭐备选 | 快 | 是 | 否（拖拽即传） | 个人版永久免费 | 邮箱注册 |
| GitHub Pages | 慢/常打不开 | 是 | 是 | 无限 | 邮箱 |
| Cloudflare Pages | 较好 | 是 | 否 | 无限流量 | 邮箱（官网可能打不开） |

---

## 方案一：腾讯云 CloudBase（推荐，国内最快）

**特点**：腾讯云 CDN 节点，全国访问极快；默认域名 `*.tcloudbaseapp.com` 已备案，免备案；免费额度完全够用。

### 步骤

**1. 注册腾讯云并进入云开发**

- 访问 https://cloud.tencent.com 注册（可用微信扫码登录，需完成实名认证）
- 登录后顶部搜索「云开发 CloudBase」→ 进入控制台
- 或直接访问：https://console.cloud.tencent.com/tcb

**2. 创建环境**

- 点击「新建环境」
- 环境名随意填（如 `beidanci`）
- 环境 ID 系统自动生成，记下来
- 计费方式选「按量计费」（免费额度内不扣费）
- 开通静态托管服务（勾选即可，免费）
- 点击「立即开通」

**3. 上传静态文件**

- 环境创建完成后，左侧菜单点击「静态网站托管」
- 点击「上传文件」
- **选择文件**：打开项目目录 `d:\Trae CN\trae_projects\beidanci`，把以下三项选中上传：
  - `index.html`
  - `css/` 整个文件夹
  - `js/` 整个文件夹
- 等待上传完成

**4. 获取访问地址**

- 静态网站托管页面顶部会显示默认域名，形如：
  `https://你的环境id-xxxx.tcloudbaseapp.com`
- 点击即可访问，永久有效

**5. 以后更新代码**

- 重新进入静态网站托管页面
- 删除旧文件，上传新文件即可
- 也可以用「文件夹覆盖上传」功能

> ⚠️ 免费额度：1GB 存储 + 5GB CDN 流量/月，个人家庭使用完全足够。超出后按流量计费也很便宜（约 0.15 元/GB）。

---

## 方案二：AxureShow（备选，无实名、最快上线）

**特点**：邮箱注册无需实名；拖拽 ZIP 即上传生成链接；永久免费。

### 步骤

**1. 注册账号**

- 访问 https://www.axureshow.com
- 右上角注册（邮箱注册即可，无需实名）

**2. 打包项目**

- 在项目目录 `d:\Trae CN\trae_projects\beidanci` 中，**选中以下三项**：
  - `index.html`
  - `css/` 文件夹
  - `js/` 文件夹
- 右键 → 发送到 → 压缩(zipped)文件夹
- 得到 `beidanci.zip`（名称随意）

**3. 上传并发布**

- 登录 AxureShow
- 把 `beidanci.zip` **直接拖拽到上传区域**
- 系统自动识别入口页面 `index.html`
- 点击「发布」

**4. 获取访问链接**

- 发布完成后自动生成公开访问链接，形如：
  `https://axureshow.com/xxxxx`
- 直接分享使用

**5. 以后更新**

- 在项目列表找到该项目 → 重新上传新的 ZIP → 覆盖发布
- 原有链接保持不变

> ⚠️ 免费版：个人基础功能永久免费，无广告。如需密码访问/私密等高级功能可升级付费版。

---

## 方案三：Vercel（若你能登录国际网站）

如果 Cloudflare 你打不开但 Vercel（https://vercel.com）可以访问，也可以用它，支持直接上传或 GitHub 自动部署。

---

## 安全说明

Supabase 的 **anon key** 是公开的（本来就设计为写在前端），安全性由 RLS（行级安全策略）保证。
已在 [supabase.sql](supabase.sql) 中启用 RLS 并配置了全表开放策略，使用没有问题。
